#include <studio.h>
#include <stdlib.h>
#include <string.h>
#include "caesar.h"

int convert_key(char *key){
	int key=atoi(key);//convert the key to a number
	//iterate through the key
	for(int i=0;i<strlen(key);i++){
		if(){
		}
	
	}
	


}
